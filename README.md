# Preview

Static design study. Runs entirely in the browser.

## Local

Open `index.html` directly or serve locally:

```
python3 -m http.server 8000
```

## Structure

- `index.html` — markup
- `styles.css` — layout, themes
- `app.js` — state, parser, rendering
